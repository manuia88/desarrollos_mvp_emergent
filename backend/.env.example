# Backend environment configuration — copy to backend/.env and fill real values
# REAL VALUES MUST NEVER BE COMMITTED. Always use placeholders here.

# MongoDB
MONGO_URL=mongodb://localhost:27017
DB_NAME=desarrollosmx

# JWT secret (generate with: python -c "import secrets;print(secrets.token_urlsafe(48))")
JWT_SECRET=REPLACE_WITH_RANDOM_48_BYTE_SECRET

# Seed admin (used only on first boot)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# Emergent universal LLM key (Claude Sonnet 4.5 + GPT + Gemini)
# Opcional · si vacío o placeholder, Studio usa copy mock determinístico (FAIL-SOFT)
EMERGENT_LLM_KEY=REPLACE_WITH_EMERGENT_LLM_KEY

# Mapbox token (signed up at https://account.mapbox.com/access-tokens/)
MAPBOX_TOKEN=REPLACE_WITH_MAPBOX_PUBLIC_TOKEN

# ─── Neo4j Knowledge Graph (W5.12 Parte 1) ───────────────────────────────────
# Vacios o ausentes → backend arranca igual y KG_AVAILABLE=False (fallback relational).
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme_in_prod

# Default WhatsApp deep-link fallback (E.164 without +)
DMX_FALLBACK_WHATSAPP=525500000000

# DMX Studio engine selectors (optional — defaults to stub)
# Set to "fal" or "replicate" once real video keys are configured
STUDIO_VIDEO_ENGINE=stub
# Set to "openai-full" to enable 100 unique image generations per ad batch
STUDIO_ADS_ENGINE=openai-stub

# Future engine keys (uncomment + fill when ready)
# FAL_API_KEY=REPLACE_WITH_FAL_API_KEY
# REPLICATE_API_TOKEN=REPLACE_WITH_REPLICATE_API_TOKEN
# ELEVENLABS_API_KEY=REPLACE_WITH_ELEVENLABS_API_KEY

# ─── IE Engine — Phase A ──────────────────────────────────────────────────────
# Encryption key for stored data-source credentials (Fernet base64 urlsafe 32 bytes).
# Generate with: python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"
# If unset, derives from JWT_SECRET (NOT recommended for production).
IE_FERNET_KEY=

# API keys (optional — sources fall back to "blocked" status when absent)
IE_NOAA_API_KEY=
IE_INEGI_TOKEN=
IE_BANXICO_TOKEN=
IE_AIRROI_API_KEY=
# [AUD-026] Freno de emergencia AirROI (API de PAGO). Default = habilitado. Pon AIRROI_ENABLED=false
# para que NINGUNA llamada salga a la API (fetch/test_connection devuelven stub) — apagar/prender es
# solo cambiar este env, sin deploy. Las vías legítimas ya están capadas (1/zona/mes + tope 400/mes).
AIRROI_ENABLED=true

# W5.20 — External Insights Ingest (12 global sources)
# FRED St. Louis Fed (US housing · Case-Shiller). Free key: https://fred.stlouisfed.org/docs/api/api_key.html
IE_FRED_API_KEY=
# Numbeo Property Index (global 500+ ciudades). Free tier limitado: https://www.numbeo.com/api/
IE_NUMBEO_API_KEY=

# CKAN resource IDs (datos.cdmx.gob.mx)
IE_DATOS_CDMX_BASE_URL=https://datos.cdmx.gob.mx/api/3/action
IE_FGJ_CDMX_RESOURCE_ID=
IE_SACMEX_RESOURCE_ID=
IE_LOCATEL_RESOURCE_ID=

# Keyless public URLs
IE_OSM_OVERPASS_URL=https://overpass-api.de/api/interpreter
IE_CONAGUA_SMN_URL=https://smn.conagua.gob.mx/tools/GUI/webservices/
IE_GTFS_CDMX_URL=

# WMS/WFS
IE_CENAPRED_WFS_URL=
IE_ATLAS_CDMX_BASE_URL=https://geoinformacionpublica.cdmx.gob.mx

# Manual upload storage (local dev — switch to S3 in prod)
IE_UPLOAD_DIR=/uploads/ie_engine

# H2 — Dubai expansion
IE_REELLY_API_KEY=

# Batch 25 — Image Embeddings Pipeline (set to true to enable real vector search)
IMAGE_EMBEDDINGS_ENABLED=false

# Batch 25 — Resend email alerts (https://resend.com)
RESEND_API_KEY=REPLACE_WITH_RESEND_API_KEY

# Batch 30 — Sparklines precio histórico (false=sintético default, true=db.unit_price_history)
PRICE_HISTORY_REAL_DATA=false

# ─── W4.18.3 — Private Beta Gate ──────────────────────────────────────────
# PRIVATE_BETA_MODE=true → /api/auth/register devuelve 403 code=private_beta
# (signup broker con invite code via /api/auth/signup-broker sigue activo)
PRIVATE_BETA_MODE=true
PRIVATE_BETA_INVITE_EXPIRES_DAYS=90
PRIVATE_BETA_BROKER_PORTAL_URL=/broker-portal

# ─── W4.18.2A.0 — PostHog (cliente only · backend NO consume) ─────────────────
# Documentación de las 2 ENV vars consumidas por /app/frontend/src/lib/posthog.js.
# Configurar en /app/frontend/.env (NO aquí). Backend nunca instancia PostHog.
#   REACT_APP_POSTHOG_API_KEY=phc_xxx
#   REACT_APP_POSTHOG_HOST=https://us.i.posthog.com

# W4.9.6 — Luma AI Gaussian Splatting (optional; missing key → stub mode)
LUMA_API_KEY=
LUMA_API_BASE=https://webapp.lumalabs.ai/api/v3
TOUR3DGS_STORAGE_PATH=/app/backend/storage/3dgs
TOUR3DGS_STORAGE_BUCKET=

# W5.5 Parte 1 — Live Pulse + frequencia configurable
LIVE_PULSE_FREQUENCY=weekly
APIFY_TRENDS_REAL=false

# W5.22 Z.1 — Studio Brand Kit + Asset Library (Cloudflare R2)
# Si vacios → backend funciona en modo stub (presigned URLs null), no rompe nada
CLOUDFLARE_R2_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY=
CLOUDFLARE_R2_SECRET_KEY=
CLOUDFLARE_R2_BUCKET=dmx-studio-assets
STUDIO_IMPORTER_USER_AGENT=DMX-Studio-Importer/1.0 (+https://desarrollosmx.io/legal/importer)

# W5.22 Z.8 — Studio Landing Pages (custom domain hooks · Z.9 prep)
LANDING_DEFAULT_DOMAIN=desarrollosmx.io
LANDING_CUSTOM_DOMAIN_HOOKS_ENABLED=false

# ─── [AUD-001] Vars detectadas por la auditoría (documentar valor real antes de prod) ───

# ACCURACY*
ACCURACY_DRIFT_WEIGHT=            # usada en backend/live_pulse_engine.py

# ADMIN*
ADMIN_ANALYTICS_TOKEN=            # usada en backend/routes/public.py

# AGENT*
AGENT_WORKFORCE_DAILY_CAP_PER_TENANT=            # usada en backend/agent_workforce/agent_common.py

# AI*
AI_BUDGET_DEFAULT_CAP_MXN=            # usada en backend/ai_budget.py
AI_DISABLED=            # usada en backend/ai_budget.py
AI_SEARCH_MAX_PER_DAY=            # usada en backend/routes/public.py
AI_SEARCH_MAX_PER_HOUR=            # usada en backend/routes/public.py

# ALERT*
ALERT_EMAIL=            # usada en backend/ai_budget.py

# ANTHROPIC*
ANTHROPIC_API_KEY=            # usada en backend/agentic_crm/argumentario_engine.py

# APIFY*
APIFY_API_TOKEN=            # usada en backend/buyer_score_engine.py
APIFY_GOOGLE_TRENDS_ACTOR=            # usada en backend/apify_trends_engine.py
APIFY_MEMORY_MB=            # usada en backend/apify_trends_engine.py
APIFY_TIMEOUT_SECS=            # usada en backend/apify_trends_engine.py
APIFY_TOKEN=            # usada en backend/apify_trends_engine.py
APIFY_TRENDS_CACHE_DAYS=            # usada en backend/apify_trends_engine.py

# APP*
APP_PUBLIC_BASE=            # usada en backend/resend_engine.py

# ARGUMENTARIO*
ARGUMENTARIO_MODEL=            # usada en backend/agentic_crm/argumentario_engine.py

# ASISTENTE*
ASISTENTE_MODEL=            # usada en backend/asistente_engine.py

# ASSET*
ASSET_UPLOAD_DIR=            # usada en backend/dev_assets.py

# ATLAS*
ATLAS_RESENAS_BASE_URL=            # usada en backend/reviews_residents_engine.py
ATLAS_RESENAS_ENABLED=            # usada en backend/reviews_residents_engine.py

# AUTOPILOT*
AUTOPILOT_CONFIDENCE_MIN=            # usada en backend/auto_pilot_engine.py
AUTOPILOT_DAILY_CAP=            # usada en backend/auto_pilot_engine.py

# BACKEND*
BACKEND_PUBLIC_URL=            # usada en backend/drive_engine.py

# BANXICO*
BANXICO_TOKEN=            # usada en backend/banxico_rates.py

# BEHAVIORAL*
BEHAVIORAL_IP_SALT=            # usada en backend/behavioral_tracking_engine.py

# BROCHURE*
BROCHURE_STORAGE_PATH=            # usada en backend/brochure_renderer.py

# CASAMENTERA*
CASAMENTERA_CRON_TOKEN=            # usada en backend/routes/casamentera.py

# CEREBRO*
CEREBRO_ENABLED=            # usada en backend/cerebro/__init__.py

# CHURN*
CHURN_SALES_USER_IDS=            # usada en backend/churn_prediction_cron.py

# CLEARBIT*
CLEARBIT_API_KEY=            # usada en backend/lead_enrichment_engine.py

# CLOUDFLARE*
CLOUDFLARE_R2_PUBLIC_URL=            # usada en backend/studio_asset_library.py

# CONFIDENCE*
CONFIDENCE_DAILY_CAP=            # usada en backend/conversation_engine.py

# CONSTRUCTION*
CONSTRUCTION_QUALITY_CACHE_TTL_DAYS=            # usada en backend/construction_quality_engine.py

# CONVERSATION*
CONVERSATION_HAIKU_MODEL=            # usada en backend/conversation_cost_optimizer.py
CONVERSATION_MODEL=            # usada en backend/conversation_engine.py
CONVERSATION_OPUS_MODEL=            # usada en backend/conversation_cost_optimizer.py
CONVERSATION_SONNET_MODEL=            # usada en backend/conversation_cost_optimizer.py

# CORS*
CORS_ORIGINS=            # usada en backend/server.py

# COST*
COST_OPT_LONG_HISTORY=            # usada en backend/conversation_cost_optimizer.py
COST_OPT_SHORT_TEXT=            # usada en backend/conversation_cost_optimizer.py

# CRON*
CRON_SECRET=            # usada en backend/routes/copiloto_flywheel.py

# CSRF*
CSRF_ALLOWED_ORIGINS=            # usada en backend/server.py

# DI*
DI_UPLOAD_DIR=            # usada en backend/document_intelligence.py

# DIRECTOR*
DIRECTOR_MAX_TOKENS_IN_T1=            # usada en backend/director_agent_engine.py
DIRECTOR_MAX_TOKENS_OUT_T1=            # usada en backend/director_agent_engine.py
DIRECTOR_MODEL=            # usada en backend/agentic_crm/disc_inferencer_engine.py

# DISCORD*
DISCORD_WEBHOOK_URL=            # usada en backend/marketing_mcp_engine.py

# DMX*
DMX_API_KEY_PREFIX=            # usada en backend/public_api_auth.py
DMX_DEV_MODE=            # usada en backend/routes/auth.py
DMX_ENV=            # usada en backend/cerebro/executors.py
DMX_MEMORY_DIR=            # usada en backend/studio_copy_generator/loader.py
DMX_ORG_ID=            # usada en backend/routes/whatsapp_copiloto.py
DMX_PUBLIC_URL=            # usada en backend/routes/watchlist.py
DMX_RELEASE=            # usada en backend/observability.py

# ELEVEN*
ELEVEN_LABS_API_KEY=            # usada en backend/voice_atlax_engine.py
ELEVEN_VOICE_ID=            # usada en backend/tests/wave4/test_voice_atlax_engine_unit.py

# ELEVENLABS*
ELEVENLABS_API_KEY=            # usada en backend/adapters/tts/elevenlabs.py

# ERP*
ERP_WEBHOOK_SECRET=            # usada en backend/routes/dev_batch1.py

# FAL*
FAL_KEY=            # usada en backend/studio_engines.py

# FEATURE*
FEATURE_GATING_ENFORCED=            # usada en backend/feature_gate_engine.py

# FOUNDER*
FOUNDER_ALERT_EMAIL=            # usada en backend/anomaly_detection_engine.py

# FOURSQUARE*
FOURSQUARE_API_KEY=            # usada en backend/reviews_residents_engine.py

# FREE*
FREE_AUDIT_STORAGE=            # usada en backend/free_audit_engine.py

# FRONTEND*
FRONTEND_BASE_URL=            # usada en backend/routes/auth.py
FRONTEND_PUBLIC_URL=            # usada en backend/routes/dev_batch4_3.py
FRONTEND_URL=            # usada en backend/drive_engine.py

# GOOGLE*
GOOGLE_CSE_CX=            # usada en backend/reputation_monitor_engine.py
GOOGLE_CSE_KEY=            # usada en backend/reputation_monitor_engine.py
GOOGLE_INGEST_TOKEN=            # usada en backend/routes/public.py
GOOGLE_MAPS_API_KEY=            # usada en backend/google_places_ingest.py
GOOGLE_OAUTH_CLIENT_ID=            # usada en backend/drive_engine.py
GOOGLE_OAUTH_CLIENT_SECRET=            # usada en backend/drive_engine.py
GOOGLE_OAUTH_REDIRECT_URI=            # usada en backend/drive_engine.py
GOOGLE_PLACES_API_KEY=            # usada en backend/reviews_residents_engine.py

# HOOK*
HOOK_PREDICTOR_DAILY_CAP_PER_TENANT=            # usada en backend/hook_predictor_engine.py
HOOK_PREDICTOR_THRESHOLD=            # usada en backend/hook_predictor_engine.py

# IE*
IE_COLONIAS_CDMX_RESOURCE_ID=            # usada en backend/colonias_catalog.py
IE_COLONIAS_CDMX_URL=            # usada en backend/colonias_catalog.py
IE_DISABLE_CRON=            # usada en backend/scheduler_ie.py
IE_INEGI_AREA=            # usada en backend/gov_data_mx_engine.py
IE_INEGI_INDICADOR=            # usada en backend/gov_data_mx_engine.py
IE_SHF_XLSX_URL=            # usada en backend/shf_engine.py
IE_SIG_WFS_URL=            # usada en backend/sig_catastro_engine.py
IE_VALORES_UNITARIOS_2026_URL=            # usada en backend/valores_unitarios_engine.py

# LEAD*
LEAD_CAPTURE_SECRET=            # usada en backend/routes/dev_batch1.py
LEAD_ENRICHMENT_CACHE_TTL_DAYS=            # usada en backend/lead_enrichment_engine.py
LEAD_ENRICHMENT_DAILY_CAP_PER_TENANT=            # usada en backend/lead_enrichment_engine.py
LEAD_ENRICHMENT_SOURCES_ORDER=            # usada en backend/lead_enrichment_engine.py

# LFPDPPP*
LFPDPPP_SALT=            # usada en backend/buyer_coach_engine.py

# LINKEDIN*
LINKEDIN_AUTHOR_URN=            # usada en backend/marketing_mcp_engine.py
LINKEDIN_OAUTH=            # usada en backend/marketing_mcp_engine.py
LINKEDIN_OAUTH_CLIENT_ID=            # usada en backend/services/linkedin_import.py
LINKEDIN_TOKEN=            # usada en backend/conversation_channels/linkedin.py

# LLM*
LLM_TIMEOUT_SECONDS=            # usada en backend/llm_client.py

# LOG*
LOG_LEVEL=            # usada en backend/lead_nurture_engine.py

# MARKETPLACE*
MARKETPLACE_TEMPLATES_CLONE_CAP_HOURLY=            # usada en backend/routes/marketplace_templates.py
MARKETPLACE_TEMPLATES_PUBLISH_CAP_DAILY=            # usada en backend/routes/marketplace_templates.py
MARKETPLACE_TEMPLATE_REVENUE_DMX_PCT=            # usada en backend/marketplace_templates_engine.py

# META*
META_APP_ID=            # usada en backend/social_ads_oauth.py
META_APP_REVIEW_APPROVED=            # usada en backend/social_ads_oauth.py
META_APP_SECRET=            # usada en backend/lead_capture_engine.py
META_IG_TOKEN=            # usada en backend/conversation_channels/instagram.py
META_PAGE_TOKEN=            # usada en backend/conversation_channels/instagram.py
META_VERIFY_TOKEN=            # usada en backend/routes/lead_capture.py
META_WA_PHONE_ID=            # usada en backend/asistente_engine.py
META_WA_TOKEN=            # usada en backend/asistente_engine.py

# MONGO*
MONGO_CONNECT_TIMEOUT_MS=            # usada en backend/server.py
MONGO_MAX_POOL_SIZE=            # usada en backend/server.py
MONGO_MIN_POOL_SIZE=            # usada en backend/server.py
MONGO_SERVER_SELECTION_MS=            # usada en backend/server.py
MONGO_SOCKET_TIMEOUT_MS=            # usada en backend/server.py

# NURTURE*
NURTURE_THROTTLE_DAYS=            # usada en backend/newsletter_pulse_engine.py

# OAUTH*
OAUTH_TOKEN_ENCRYPTION_KEY=            # usada en backend/oauth_calendar.py

# OPENAI*
OPENAI_API_KEY=            # usada en backend/llm_client.py

# PDL*
PDL_API_KEY=            # usada en backend/lead_enrichment_engine.py

# PEDRA*
PEDRA_API_BASE=            # usada en backend/dev_assets.py
PEDRA_API_KEY=            # usada en backend/dev_assets.py

# POSTHOG*
POSTHOG_HOST=            # usada en backend/connector_registry.py
POSTHOG_KEY=            # usada en backend/connector_registry.py

# PUBLIC*
PUBLIC_BASE_URL=            # usada en backend/cma_pdf_renderer.py
PUBLIC_URL=            # usada en backend/routes/advisor.py

# QA*
QA_BASE=            # usada en scripts/qa_journey_dev.py

# REPLICATE*
REPLICATE_API_TOKEN=            # usada en backend/studio_engines.py

# REPUTATION*
REPUTATION_ALERT_THRESHOLD=            # usada en backend/reputation_monitor_engine.py
REPUTATION_BRAND_KEYWORDS=            # usada en backend/reputation_monitor_engine.py

# RESEND*
RESEND_FROM=            # usada en backend/newsletter_pulse_engine.py
RESEND_FROM_LEAD_NURTURE=            # usada en backend/agentic_crm/reply_classifier_engine.py
RESEND_FROM_NOTIFICATIONS=            # usada en backend/notifications_engine.py
RESEND_REPLY_TO=            # usada en backend/resend_engine.py
RESEND_WEBHOOK_SECRET=            # usada en backend/routes/agentic_crm.py

# REVIEWS*
REVIEWS_RESIDENTS_CACHE_TTL_DAYS=            # usada en backend/reviews_residents_engine.py

# SENTRY*
SENTRY_DSN=            # usada en backend/connector_registry.py

# SOC*
SOC_FRANCHISE_CACHE_TTL_DAYS=            # usada en backend/soc_franchise_engine.py
SOC_REVENUE_TARGET_30D_MXN=            # usada en backend/soc_franchise_engine.py

# SOCIAL*
SOCIAL_ADS_ENCRYPTION_KEY=            # usada en backend/social_ads_oauth.py
SOCIAL_ADS_FERNET_KEY=            # usada en backend/social_ads_oauth.py
SOCIAL_ADS_MAX_CONNECTIONS_PER_TENANT=            # usada en backend/social_ads_engine.py
SOCIAL_ADS_OAUTH_REDIRECT_URI=            # usada en backend/routes/social_ads.py
SOCIAL_CARDS_STORAGE=            # usada en backend/social_cards_engine.py

# STATE*
STATE_OF_CDMX_STORAGE=            # usada en backend/state_of_cdmx_engine.py

# STRIPE*
STRIPE_API_KEY=            # usada en backend/stripe_billing_engine.py
STRIPE_SECRET_KEY=            # usada en backend/stripe_billing_engine.py
STRIPE_WEBHOOK_SECRET=            # usada en backend/server.py

# STUDIO*
STUDIO_COPY_ANTHROPIC_MODEL=            # usada en backend/studio_copy_generator/llm_client.py
STUDIO_COPY_MODEL=            # usada en backend/studio_copy_generator/llm_client.py
STUDIO_STORAGE_PATH=            # usada en backend/studio_engines.py
STUDIO_TTS_ENGINE=            # usada en backend/routes/studio.py
STUDIO_VIDEO_DAILY_CAP_USD=            # usada en backend/ai_budget.py
STUDIO_VIDEO_OUTPUT_PATH=            # usada en backend/multiratio_renderer.py

# TEST*
TEST_API_URL=            # usada en backend/tests/test_batch11.py
TEST_BASE_URL=            # usada en backend/tests/test_batch14.py

# TG*
TG_BOT_TOKEN=            # usada en backend/marketing_mcp_engine.py
TG_CHAT_ID=            # usada en backend/marketing_mcp_engine.py

# TIKTOK*
TIKTOK_TOKEN=            # usada en backend/conversation_channels/tiktok.py

# TRAFFIC*
TRAFFIC_BRIEFING_TTL_MIN=            # usada en backend/services/traffic_briefing.py

# TRUSTED*
TRUSTED_PROXY_HOPS=            # usada en backend/ratelimit.py

# TWILIO*
TWILIO_ACCOUNT_SID=            # usada en backend/asistente_engine.py
TWILIO_AUTH_TOKEN=            # usada en backend/asistente_engine.py
TWILIO_WA_FROM=            # usada en backend/whatsapp_engine.py

# URL*
URL_GUARD_ALLOWLIST=            # usada en backend/services/url_guard.py

# WA*
WA_PROVIDER=            # usada en backend/cerebro/executors.py

# WHATSAPP*
WHATSAPP_PROVIDER=            # usada en backend/asistente_engine.py
WHATSAPP_VPS_READY=            # usada en backend/conversation_channels/whatsapp_stub.py

# WIZARD*
WIZARD_STORAGE_PATH=            # usada en backend/routes/wizard.py

# X*
X_BEARER_TOKEN=            # usada en backend/marketing_mcp_engine.py

# YOUTUBE*
YOUTUBE_TOKEN=            # usada en backend/conversation_channels/youtube.py
